---
import {metrics} from '../data/site';
---
<section class="metrics" aria-label="Career experience"><div class="container metrics-grid">{metrics.map(metric=><div><strong>{metric.value}</strong><span>{metric.label}</span></div>)}</div></section>
